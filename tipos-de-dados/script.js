console.log('oi');

const nome = 'André';
const sobrenome = 'Rafael';
const nomeCompleto = nome + ' ' + sobrenome;
const idade = 30;
console.log(typeof nome);
console.log(typeof idade);
console.log(nomeCompleto);

let gols = 1000;
let frase = `Romário fez ${gols} gols`;
console.log(frase);

// Exercício

// Declare uma variável contendo uma string
const string = 'Origamid';

// Declare uma variável contendo um número dentro de uma string
const numberString = '30';

// Declare uma variável com a sua idade
const minhaIdade = 30;
console.log(numberString);

// Declare duas variáveis, uma com seu nome
// e outra com seu sobrenome e some as mesmas
const meuNome = 'Thiago';
const meuSobrenome = 'Pederzolli Machado da Silva';
const meuNomeCompleto = `${meuNome} ${meuSobrenome}`;
console.log(meuNomeCompleto);

// Coloque a seguinte frase em uma variável: It's time
const time = "It's time";

// Verifique o tipo da variável que contém o seu nome
console.log(typeof meuNome);